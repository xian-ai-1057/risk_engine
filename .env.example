# 環境變數設定範本 — 複製為 .env 後依需要修改。
# .env 不會被提交到版控（見 .gitignore）；未設定的項目皆會使用程式內建預設值。
# 真正的 shell 環境變數（export ...）優先於 .env 內的值。

# ── Web Demo 伺服器設定（web/app.py::main，`python -m web` / `risk-web`） ──
# RISK_WEB_HOST=127.0.0.1
# RISK_WEB_PORT=8000
# RISK_WEB_RELOAD=false

# ── Web Demo 資源目錄（web/services.py::resource_dir） ──
# 指標 xlsx + 兩份 user prompt 模板所在目錄；預設 <repo>/deploy
# RISK_WEB_RESOURCE_DIR=deploy

# ── LLM 端點設定（CLI/EXE --generate 與 Web Demo 生成敘述段落共用） ──
# OpenAI 相容端點；三者需一起設定，缺一則生成會失敗
# （CLI 回 CONFIG_ERROR；Web Demo 回 400 並在畫面提示）。
# Web Demo 介面不再輸入金鑰，一律由這三個變數提供。
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
# LLM_MODEL=gpt-4o
